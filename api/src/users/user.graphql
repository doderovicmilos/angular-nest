type User {
  id: ID!
  username: String!
  roles: [String!]
}

input UserDto {
  username: String!
  roles: [String!]
}

type Query {
  hello: String!
  users: [User!]
}

type Mutation {
  createUser(input: UserDto!): User
}